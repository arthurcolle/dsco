# Kukulkan — weather market intelligence stack
# Core
click>=8.0
requests>=2.28
rich>=13.0

# NWP data access
herbie-data>=2024.8       # Multi-source HRRR/GFS/NAM via AWS/Google/NOMADS
# sharppy                 # BUFKIT parser — install from git: pip install git+https://github.com/sharppy/SHARPpy

# Observations
metar>=1.11               # Full METAR remarks parser (SLP, peak wind, 6/24h T, precip)

# ML / calibration
lightgbm>=4.0
scikit-learn>=1.3
scoringrules>=0.8         # CRPS for 25+ distributions, JAX/PyTorch differentiable
quantile-forest>=1.3      # Quantile regression forests, sklearn-compatible
river>=0.21               # Online ML: SNARIMAX, ADWIN drift detection
pykalman>=0.9.7           # EM-learned Kalman filter for real-time bias tracking
mapie>=0.9                # Conformal prediction intervals (CQR)
dirichletcal>=0.3         # Multi-class calibration on the simplex

# Distributional / statistical
pyextremes>=2.3           # EVT: GEV/GPD for tail bucket probabilities
pyvinecopulib>=0.7        # Vine copulas for joint city dependence

# Satellite + solar
pvlib>=0.11               # Clear-sky GHI → cloud attenuation factor
goes2go>=2024.1           # GOES cloud mask/DSR from AWS S3

# Trading
pykalshi>=1.0             # Kalshi WebSocket streaming + typed API, RSA-PSS auth

# TUI
textual>=0.80             # Production TUI: DataTable, Sparkline, CSS styling
textual-plotext>=0.3

# Misc
numpy>=1.24
scipy>=1.11
pandas>=2.0
xarray>=2024.1

# Visualization diagnostics
matplotlib>=3.8
Pillow>=10
