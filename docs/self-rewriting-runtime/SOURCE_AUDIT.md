# Source mechanisms behind the replacement prompts

Inspected directly from the paths supplied by the user. This is a static implementation audit plus one reported offline cognitive wiring smoke; it is not a claim of measured cognitive improvement. Python modules with import-time provider setup were read without importing them or launching inference. Generated caches and `__pycache__` are not treated as source.

The two concatenated paths in the request resolve to separate files: `dsco-mcp-openrouter-client/universal_dspy_multihop.py` and `dsco-mcp-openrouter-client/verify_cognitive_integration.py` under `/Users/arthurcolle/Dsco`.

| Source and symbol | Observed mechanism | What an implementation prompt must add |
| --- | --- | --- |
| `dsco-mcp-openrouter-client/universal_dspy_multihop.py`: `UniversalMultiHopRAG.forward`, `NotesExtractor` | Fixed-hop query/retrieve/extract/synthesize; contexts truncated to first 500 characters; notes and confidence are model outputs | Typed evidence references, failed-hop accounting, informative next queries, novelty stopping and supported conclusions |
| Same file: `UniversalContentRetriever.retrieve` | Sequential heterogeneous retrievers; results sorted by their supplied scores | Concurrent bounded collection, comparable ranking and preserved source independence |
| `dsco-mcp-openrouter-client/cognitive_architecture.py`: `ActionStack`; `cognitive_integration.py`: `CognitiveHandlers` | Action dependencies order stages; downstream handlers use the original message rather than prior stage results | Explicit typed output bindings, failure propagation and reflection that changes subsequent actions |
| `dsco-mcp-openrouter-client/verify_cognitive_integration.py` | A mock client exercises imports, handlers and counters and prints success | Assertions about actual stage semantics and real runtime wiring; a mock response is not reasoning evidence |
| `chunked_analyzer.py`: `ChunkKnowledge`, `create_chunks`, `_aggregate_results` | Extracted claims and entities from overlapping text, lowercase-string deduplication; bias pass sees only part of each default chunk | Exact intervals, overlap invariance, attribution, complete coverage accounting and contradiction preservation |
| `analysis_cache.py`: `AnalysisCache._get_cache_key` | Path/model/chunk configuration cache key | Content, extraction and program-version dependencies so edited evidence invalidates derived conclusions |
| `pdf_cache.py`: `PDFTextCache`; `pdf_processor.py`: `PDFVisionAnalyzer` | Content-cached text and separately rendered vision pages | Shared page provenance, targeted visual checks and deterministic table/equation validation |
| `cognitive_agent/core.py`: `CognitiveAgentProgram`, `select_lenses` | One structured predictor plus substring/static selection rules | Typed executable methods selected from task requirements and observed marginal benefit |
| `cognitive_agent/deep.py`: `DeepReasoningProgram`, `_assemble` | Scan/method/lens/critique/synthesis stages; final confidence remains model-produced | Validated evidence, real intermediate bindings, enforced correction propagation and measured outcome calibration |
| `comprehensive_reasoning_system.py`: `HybridReasoningPipeline.forward` | Computes corrected analyses, then returns original `multi.synthesis` | A material accepted correction must revise or invalidate dependent final assertions and executable candidates |
| Same file: `ProbabilisticReasoningSignature`, `ScientificReasoningWorkflow` | Posterior signature lacks competing likelihood/evidence probability; workflow supplies placeholder prior/likelihood | Complete probability inputs, dependency-aware updates, deterministic math or an explicit unknown result |
| `debiased_reasoning_modules.py`: `ReferenceClassForecasting`; `debiased_applications.py`: `DebiasedRAG` | LM-produced cohort statistics and placeholder retrieval fallback in prototype paths | Versioned empirical data and empty/failed retrieval represented as missing evidence |
| `specialized_teams.py`: `ReasoningToolkit.reduce_biases`; `eval_bias_reduction.py`: `evaluate_test_case` | Failed verification can become zero remaining biases; missing after-analysis can resemble success | Unknown/failure states that cannot be scored as improvement, with independent expected outcomes |
| `dspy_metaprogramming.py`: `CodeSandbox`, `ASTAnalyzer`, `MetaToolRegistry` | `exec` in shared Python state, AST edits, source-backed callable composition | Native code contracts, real execution isolation where required, effect routing, lifetimes and semantic tests |
| `dspy_streaming_tools.py`: `ToolExecutor` | Dependency-ready waves and result passing, including records for failed predecessors | Distinguish successful validated values from mere completed calls; do not let failed evidence satisfy success dependencies |
| `swarm_router/pipeline.py`: `FourModelHierarchicalSwarm` | Typed planning, heterogeneous child execution, review and synthesis | Enforce independent success criteria and retain contradictory evidence through reduction |
| `swarm_router/scaling.py`: `ScaledSwarm` | Prefix-based agreement and answer-length fallback | Task-specific equivalence, independently checked outcomes and stopping rules resistant to correlated wrong answers |
| `swarm_router/optimize.py`: `swarm_metric`, `compile_with_mipro`, `compile_with_gepa` | Actual optimization wrappers with substring/nonempty baseline metric | Oracles that reject answer stuffing and code that overfits demonstrations; preserve unseen evaluation families |
| `dsco_core/agents/sqlite.py`, `duckdb.py`, `embeddings.py` | Some storage/embedding examples use dictionaries, simulated SQL or hash-derived vectors | Real query execution and measured semantic retrieval before claims of knowledge-based reasoning |
| `compare_models.py`: `paired_comparison` | Positional pairing truncated to shorter result list | Stable task identity joins, missingness accounting and uncertainty estimates |

Paths prefixed with `dsco-mcp-openrouter-client/` are relative to `/Users/arthurcolle/Dsco`; all other Python paths in this table are relative to `/Users/arthurcolle/Dsco/dspy_multidimensional_reasoning_and_cognitive_bias_reduction`. Each prompt manifest carries absolute donor paths and named symbols so the observation can be checked in another session. `sources.json` records the exact source-byte hashes used to validate the package.

Current C integration foundations are `src/vm.c`, `src/plugin.c`, `src/ast.c`, `src/prompt_program.c`, `src/context_fabric.c`, `src/plan_dag.c`, `src/trace_kg.c`, `src/blackboard.c`, `src/cost_frontier.c` and the existing governed execution/event machinery. Their existence does not establish the proposed native evolution loop. In particular, `/restart` uses `execvp`, and raw VM callbacks require an explicit governed boundary before use as generated tool effects.
